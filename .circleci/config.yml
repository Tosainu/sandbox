version: 2

jobs:
  build:
    docker:
      - image: circleci/buildpack-deps
    steps:
      - run: |
        cat /proc/cpuinfo
        cat /proc/meminfo

workflows:
  version: 2
  build:
    jobs:
      - build
