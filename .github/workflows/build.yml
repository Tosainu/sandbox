name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main
        with:
          submodules: true

      - uses: actions/setup-node@main
        with:
          node-version: 14

      - run: yarn
      - run: yarn build
